// Template for `supabase-config.js`. Copy this file to `supabase-config.js`
// (same folder) and fill in your project's real values.
//
// `supabase-config.js` IS committed to git so GitHub Pages can serve a working
// build. The publishable (anon) key is safe to ship — RLS enforces all access
// control. Never put a service-role key in either file.
//
// Get these from Supabase Dashboard -> Project Settings -> API.

window.SUPABASE_CONFIG = {
  url:             "https://YOUR-PROJECT.supabase.co",
  publishableKey:  "sb_publishable_REPLACE_ME",
};
