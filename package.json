{
  "name": "irh-driver-scheduler",
  "private": true,
  "version": "0.0.0",
  "description": "Static-site scheduler for NETC Fleet Services. No build step — package.json is here only to host lint/format scripts and dev dependencies.",
  "scripts": {
    "lint":   "eslint \"js/**/*.js\"",
    "format": "prettier --write \"**/*.{js,json,html,css,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,html,css,md}\""
  },
  "devDependencies": {
    "eslint":   "^8.57.0",
    "prettier": "^3.3.3"
  }
}
